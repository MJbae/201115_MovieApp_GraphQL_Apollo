type Movie {
  id: Int!
  name: String!
  score: Int!
}

type Query {
  getMovies: [Movie]!
  getFilteredMovie(id: Int!): Movie!
}

type Mutation {
  addOneMovie(name: String!, score: Int!): Movie!
}
